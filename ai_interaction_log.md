### Task: Core Chat Application
**Prompt:** "Build a Streamlit ChatGPT-style app in progressive stages: set up the page and Hugging Face API connection, add multi-turn chat with `st.chat_message` and `st.chat_input`, add sidebar chat management, and persist each chat as a separate JSON file in the `chats/` folder."
**AI Suggestion:** The AI suggested building the app around Streamlit's native chat UI, storing all conversations in `st.session_state`, sending the full message history to the Hugging Face Inference Router for context-aware responses, and saving each chat to its own JSON file so chats can be loaded, switched, and deleted across sessions.
**My Modifications & Reflections:** The code was adapted to fit the assignment requirements by using `st.secrets["HF_TOKEN"]` safely, showing clear error messages when the token is missing or the API fails, and using the native sidebar for chat navigation. I also added a simple empty-state test flow so the API connection can be checked quickly with a "Hello!" message. The structure matches the assignment well, though I should still run the app and test multi-chat behavior manually before final submission.

### Task: Response Streaming
**Prompt:** "Update the Streamlit chat app so model replies stream token-by-token using the Hugging Face API with `stream=True`, render the response incrementally in the UI, and save the full streamed reply to chat history once streaming finishes."
**AI Suggestion:** The AI suggested switching the API call to server-sent event streaming, parsing each `data:` chunk from the response, and using Streamlit's native streaming support with `st.write_stream()` to display the reply live while collecting the full text for persistence after completion.
**My Modifications & Reflections:** The streaming logic was added to the existing chat app instead of rebuilding the app structure. I kept the saved chat format the same, but changed the response flow so the assistant message appears incrementally before the completed text is written to the JSON chat file. A short delay was also added so the streaming effect is visible even if the small model returns chunks very quickly. I still need to run the app and confirm the live streaming behavior manually.
